import { BaseCollabCard, BaseCollabCardProps } from "./BaseCollabCard";
import { FileText } from "lucide-react";

export function BlogPostCollabCard({ data }: BaseCollabCardProps) {
  return <BaseCollabCard data={data} />;
}